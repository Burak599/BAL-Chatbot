# =============================================================
# BAL Chatbot — Python Dependencies
# Install: pip install -r requirements.txt
# =============================================================

# ── AI / Embeddings ───────────────────────────────────────────

numpy>=1.26.0
faiss-cpu>=1.8.0
sentence-transformers>=3.0.0

# ── HTTP / API Requests ───────────────────────────────────────

requests>=2.31.0

# ── Web Backend / Production ──────────────────────────────────

Flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=22.0.0
Werkzeug>=3.0.0

# ── Database / Auth / Sessions ────────────────────────────────

SQLAlchemy>=2.0.0
psycopg[binary]>=3.1.0

# ── Environment Variables ─────────────────────────────────────

python-dotenv>=1.0.1

# ── Security / Crypto ─────────────────────────────────────────

cryptography>=42.0.0

curl_cffi>=0.6.0

# =============================================================
# OPTIONAL (only if needed)
# =============================================================

# CUDA FAISS:
# faiss-gpu>=1.8.0

# PyTorch ecosystem (NOT required by current code)
# torch>=2.1.0
# transformers>=4.40.0
# accelerate>=0.27.0

# CPU PyTorch example:
# pip install torch --index-url https://download.pytorch.org/whl/cpu
